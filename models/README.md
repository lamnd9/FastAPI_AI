# Models Directory
This folder contains Deep Learning model weights.

Place your trained model files here (e.g., `.pt`, `.onnx`, `.h5`, `.pkl`).

> **Note:** Model files are typically large and should be added to `.gitignore`.
